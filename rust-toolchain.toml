# Pinned so a clippy pass locally means the same thing as a clippy pass in CI.
# matrix-sync fails when either workflow pins a different one.
[toolchain]
channel = "1.94.0"
components = ["clippy", "rustfmt"]
